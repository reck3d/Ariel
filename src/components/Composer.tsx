import { ArrowUp } from 'lucide-react'; import { useEffect,useRef,useState } from 'react';
export default function Composer({onSend,disabled}:{onSend:(v:string)=>void;disabled:boolean}){const[value,setValue]=useState('');const ref=useRef<HTMLTextAreaElement>(null);useEffect(()=>{if(!disabled)ref.current?.focus()},[disabled]);const submit=()=>{const v=value.trim();if(!v||disabled)return;onSend(v);setValue('');if(ref.current)ref.current.style.height='auto'};return <div className="composer-wrap"><div className="composer"><textarea ref={ref} value={value} placeholder="Message Ariel…" rows={1} onChange={e=>{setValue(e.target.value);e.target.style.height='auto';e.target.style.height=Math.min(e.target.scrollHeight,160)+'px'}} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();submit()}}}/><button onClick={submit} disabled={!value.trim()||disabled} aria-label="Send"><ArrowUp/></button></div><small>Enter to send · Shift + Enter for a new line</small></div>}
