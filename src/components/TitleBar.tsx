import { Minus, PanelLeft, Settings, Square, X } from 'lucide-react';
export default function TitleBar({toggleSidebar,openSettings}:{toggleSidebar:()=>void;openSettings:()=>void}) { return <header className="titlebar"><div className="traffic"><button className="traffic-btn close" aria-label="Close" onClick={()=>window.ariel.windowAction('close')}><X/></button><button className="traffic-btn min" aria-label="Minimize" onClick={()=>window.ariel.windowAction('minimize')}><Minus/></button><button className="traffic-btn max" aria-label="Maximize" onClick={()=>window.ariel.windowAction('maximize')}><Square/></button></div><div className="top-actions"><button className="icon-btn" onClick={toggleSidebar} aria-label="Toggle sidebar"><PanelLeft/></button><button className="icon-btn" onClick={openSettings} aria-label="Settings"><Settings/></button></div><div className="wordmark">ARIEL</div></header> }
